# Шаг 2: Деплой на Vercel

## Что делать дальше:

### 1. Зайдите на Vercel

1. Откройте браузер
2. Зайдите на https://vercel.com
3. Нажмите **"Sign Up"** (если еще нет аккаунта) или **"Log In"**

### 2. Войдите через GitHub

1. Выберите **"Continue with GitHub"**
2. Авторизуйтесь через GitHub (разрешите доступ)
3. Вы попадете на главную страницу Vercel

### 3. Импортируйте проект

1. Нажмите кнопку **"Add New..."** (в правом верхнем углу)
2. Выберите **"Project"**
3. Вы увидите список ваших GitHub репозиториев
4. Найдите **`portfolio`** в списке
5. Нажмите **"Import"** рядом с ним

### 4. Настройте проект (обычно настройки уже правильные)

Vercel автоматически определит Next.js, но проверьте:

- **Framework Preset**: `Next.js` ✅
- **Root Directory**: `./` ✅
- **Build Command**: `npm run build` ✅
- **Output Directory**: `.next` ✅
- **Install Command**: `npm install` ✅

**Environment Variables**: оставьте пустым (не нужны для этого проекта)

### 5. Задеплойте

1. Нажмите кнопку **"Deploy"** (внизу справа)
2. Дождитесь завершения сборки (обычно 1-2 минуты)
   - Вы увидите прогресс в реальном времени
   - Можете следить за логами сборки
3. После успешного деплоя увидите:
   - ✅ **"Congratulations! Your project has been deployed"**
   - URL вашего сайта: `https://portfolio-xxxxx.vercel.app`

### 6. Проверьте сайт

1. Нажмите на URL или кнопку **"Visit"**
2. Откроется ваш сайт!
3. Проверьте:
   - Главная страница загружается
   - Все кейсы открываются
   - Изображения отображаются
   - Ссылки работают

## Что дальше?

После успешного деплоя:

### Автоматические обновления

- Каждый раз, когда вы делаете `git push` в ветку `main`, Vercel автоматически задеплоит новую версию
- Pull Request'ы получат preview URL для тестирования

### Подключение домена (опционально)

Если хотите использовать свой домен (например, `yaroslavtarasov.com`):

1. В проекте на Vercel перейдите в **Settings** → **Domains**
2. Введите ваш домен
3. Добавьте DNS записи в панели регистратора домена
4. Подробная инструкция в `DEPLOY.md` (раздел "Шаг 3: Подключение домена")

## Troubleshooting

### Если сборка упала:

1. Нажмите на failed deployment
2. Посмотрите логи ошибок
3. Частые проблемы:
   - Ошибки TypeScript → исправьте локально и сделайте `git push`
   - Отсутствующие зависимости → проверьте `package.json`

### Если сайт не открывается:

1. Проверьте, что деплой завершился успешно (зеленая галочка)
2. Попробуйте обновить страницу (Cmd+R)
3. Проверьте логи в Vercel Dashboard

---

**Готово!** Ваш сайт теперь доступен в интернете! 🎉
