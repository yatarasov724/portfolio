# Инструкция по деплою на Vercel и подключению домена

## Шаг 1: Подготовка репозитория на GitHub

### 1.1. Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name**: `portfolio` (или любое другое имя)
   - **Description**: (опционально)
   - **Visibility**: выберите **Public** или **Private**
   - НЕ ставьте галочки на "Initialize with README" (у вас уже есть код)
4. Нажмите **"Create repository"**

### 1.2. Подключите локальный репозиторий к GitHub

Выполните в терминале (замените `YOUR_USERNAME` на ваш GitHub username):

```bash
cd /Users/aroslavtarasov/portfolio

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/portfolio.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

Если возникнет ошибка аутентификации, используйте Personal Access Token:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите scope: `repo` (полный доступ к репозиториям)
4. Скопируйте токен и используйте его вместо пароля при push

## Шаг 2: Деплой на Vercel

### 2.1. Создайте аккаунт на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"**
3. Выберите **"Continue with GitHub"** (рекомендуется)
4. Авторизуйтесь через GitHub

### 2.2. Импортируйте проект

1. После входа нажмите **"Add New..."** → **"Project"**
2. Найдите ваш репозиторий `portfolio` в списке
3. Нажмите **"Import"**

### 2.3. Настройте проект

Vercel автоматически определит Next.js, но проверьте настройки:

- **Framework Preset**: Next.js (должно быть автоматически)
- **Root Directory**: `./` (оставьте как есть)
- **Build Command**: `npm run build` (по умолчанию)
- **Output Directory**: `.next` (по умолчанию)
- **Install Command**: `npm install` (по умолчанию)

**Environment Variables** (если нужны):
- Обычно для этого проекта не требуются

### 2.4. Задеплойте

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки (обычно 1-2 минуты)
3. После успешного деплоя вы получите URL вида: `https://portfolio-xxxxx.vercel.app`

### 2.5. Автоматические деплои

После первого деплоя:
- Каждый push в ветку `main` автоматически запустит новый деплой
- Pull Request'ы получат preview URL для тестирования

## Шаг 3: Подключение домена

### 3.1. Купите домен (если еще нет)

Рекомендуемые регистраторы:
- [Namecheap](https://www.namecheap.com)
- [Google Domains](https://domains.google)
- [Cloudflare](https://www.cloudflare.com/products/registrar)

### 3.2. Добавьте домен в Vercel

1. В проекте на Vercel перейдите на вкладку **"Settings"**
2. Выберите **"Domains"** в меню слева
3. Введите ваш домен (например: `yaroslavtarasov.com` или `www.yaroslavtarasov.com`)
4. Нажмите **"Add"**

### 3.3. Настройте DNS записи

Vercel покажет вам, какие DNS записи нужно добавить. Обычно это:

#### Вариант 1: Для корневого домена (yaroslavtarasov.com)

Добавьте A-запись:
- **Type**: `A`
- **Name**: `@` (или оставьте пустым)
- **Value**: IP адрес от Vercel (обычно `76.76.21.21`)

Или CNAME:
- **Type**: `CNAME`
- **Name**: `@` (или оставьте пустым)
- **Value**: `cname.vercel-dns.com`

#### Вариант 2: Для поддомена www (www.yaroslavtarasov.com)

Добавьте CNAME:
- **Type**: `CNAME`
- **Name**: `www`
- **Value**: `cname.vercel-dns.com`

### 3.4. Настройка DNS в панели регистратора

Пример для Namecheap:
1. Зайдите в аккаунт на Namecheap
2. Domain List → Manage → Advanced DNS
3. Добавьте записи, которые показал Vercel
4. Сохраните изменения

Пример для Cloudflare:
1. Зайдите в Cloudflare Dashboard
2. Выберите домен → DNS → Records
3. Добавьте записи
4. Сохраните

### 3.5. Ожидание распространения DNS

- Обычно занимает **5-30 минут**, но может занять до **48 часов**
- Проверить можно через [whatsmydns.net](https://www.whatsmydns.net)

### 3.6. Проверка SSL сертификата

Vercel автоматически выдаст SSL сертификат (HTTPS) после того, как DNS записи распространятся.

## Шаг 4: Дополнительные настройки

### 4.1. Редирект www на корневой домен (или наоборот)

В Vercel → Settings → Domains:
- Включите опцию **"Redirect www to apex"** или **"Redirect apex to www"**

### 4.2. Обновление metadata для SEO

Убедитесь, что в `app/layout.tsx` правильные метаданные:

```typescript
export const metadata: Metadata = {
  title: "Yaroslav Tarasov — Продуктовый дизайнер / UI & UX",
  description: "Продуктовый дизайнер / UI & UX. Создаю интуитивные интерфейсы...",
};
```

### 4.3. Добавление sitemap.xml и robots.txt (опционально)

Создайте файл `app/sitemap.ts`:

```typescript
import { MetadataRoute } from 'next'

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: 'https://yourdomain.com',
      lastModified: new Date(),
      changeFrequency: 'monthly',
      priority: 1,
    },
    {
      url: 'https://yourdomain.com/cases/agro-platform',
      lastModified: new Date(),
      changeFrequency: 'monthly',
      priority: 0.8,
    },
    // Добавьте другие страницы
  ]
}
```

И `app/robots.ts`:

```typescript
import { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/private/',
    },
    sitemap: 'https://yourdomain.com/sitemap.xml',
  }
}
```

## Шаг 5: Проверка работы

1. Откройте ваш домен в браузере
2. Проверьте, что все страницы загружаются
3. Проверьте, что изображения оптимизированы
4. Проверьте мобильную версию
5. Проверьте скорость загрузки через [PageSpeed Insights](https://pagespeed.web.dev/)

## Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Vercel Domain Configuration](https://vercel.com/docs/concepts/projects/domains)

## Troubleshooting

### Проблема: Домен не подключается

1. Проверьте DNS записи через [whatsmydns.net](https://www.whatsmydns.net)
2. Убедитесь, что прошло достаточно времени (до 48 часов)
3. Проверьте, что записи добавлены правильно в панели регистратора

### Проблема: Сборка падает

1. Проверьте логи в Vercel Dashboard → Deployments
2. Убедитесь, что все зависимости в `package.json`
3. Проверьте, что нет ошибок TypeScript: `npm run build`

### Проблема: Изображения не загружаются

1. Убедитесь, что изображения в папке `public/`
2. Проверьте пути к изображениям (должны начинаться с `/`)
3. Проверьте, что файлы закоммичены в git

---

**Готово!** Ваш сайт должен быть доступен по вашему домену. 🚀
